document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',String(open));});}document.querySelectorAll('[data-slider]').forEach(slider=>{const imgs=[...slider.querySelectorAll('.project-media img')];let index=0;const show=i=>{index=(i+imgs.length)%imgs.length;imgs.forEach((img,n)=>img.classList.toggle('is-active',n===index));};slider.querySelector('.prev')?.addEventListener('click',()=>show(index-1));slider.querySelector('.next')?.addEventListener('click',()=>show(index+1));let startX=0;slider.addEventListener('touchstart',e=>{startX=e.touches[0].clientX},{passive:true});slider.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX;if(Math.abs(dx)>45)show(index+(dx<0?1:-1));},{passive:true});});document.querySelectorAll('[data-form]').forEach(form=>{form.addEventListener('submit',e=>{e.preventDefault();const status=form.querySelector('.form-status');if(status)status.textContent='Спасибо, заявка принята. Мы скоро свяжемся.';form.querySelectorAll('button').forEach(b=>b.disabled=true);setTimeout(()=>form.querySelectorAll('button').forEach(b=>b.disabled=false),1600);});});document.querySelectorAll('[data-quiz]').forEach(quiz=>{const steps=[...quiz.querySelectorAll('.quiz-step')];let current=0;const prev=quiz.querySelector('[data-quiz-prev]');const next=quiz.querySelector('[data-quiz-next]');const submit=quiz.querySelector('[data-quiz-submit]');const label=quiz.querySelector('[data-quiz-step-label]');const bar=quiz.querySelector('[data-quiz-bar]');function render(){steps.forEach((s,i)=>s.classList.toggle('is-active',i===current));if(label)label.textContent='Шаг '+(current+1)+' из '+steps.length;if(bar)bar.style.width=((current+1)/steps.length*100)+'%';if(prev)prev.style.visibility=current===0?'hidden':'visible';if(next)next.style.display=current===steps.length-1?'none':'inline-flex';if(submit)submit.style.display=current===steps.length-1?'inline-flex':'none';}prev?.addEventListener('click',()=>{current=Math.max(0,current-1);render();});next?.addEventListener('click',()=>{current=Math.min(steps.length-1,current+1);render();});render();});document.querySelectorAll('[href="#lead"],[data-open-modal]').forEach(btn=>btn.addEventListener('click',e=>{e.preventDefault();document.getElementById('lead-modal')?.classList.add('is-open');}));const counters=[...document.querySelectorAll('[data-count]')];const runCounter=el=>{if(el.dataset.done)return;el.dataset.done='true';const start=Number(el.dataset.start||0);const end=Number(el.dataset.count||0);const suffix=el.dataset.suffix||'';const duration=1200;const t0=performance.now();const tick=now=>{const p=Math.min(1,(now-t0)/duration);const eased=1-Math.pow(1-p,3);const value=Math.round(start+(end-start)*eased);el.textContent=value+suffix;if(p<1)requestAnimationFrame(tick);};requestAnimationFrame(tick);};if(counters.length){const io='IntersectionObserver' in window?new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){runCounter(entry.target);io.unobserve(entry.target);}});},{threshold:.35}):null;counters.forEach(el=>io?io.observe(el):runCounter(el));}document.querySelectorAll('.modal__close,.modal').forEach(el=>el.addEventListener('click',e=>{if(e.target===el||el.classList.contains('modal__close'))document.getElementById('lead-modal')?.classList.remove('is-open');}));});